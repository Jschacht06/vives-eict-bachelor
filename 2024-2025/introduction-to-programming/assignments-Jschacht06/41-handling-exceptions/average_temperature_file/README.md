# Average Temperature from file

Copy the solution from the challenge Average Temperature and update it as follows:

* Use a file with numbers in stead of the command line arguments
* Write a file handeling code that reads the numbers from a file and make sure all file related errors are handled.
* Look up the finaly syntax of exception handling and use that to make sure the code is always ended the same way.


