# Syntax `endl`

What does `endl` do? It is often found after outputting some information to the terminal. What is the difference with `"\n"`?

## Answer

It ends the line, this means if you output something again to the console it will be on a new line instead of being pasted on the same line. There is no real difference with newline.