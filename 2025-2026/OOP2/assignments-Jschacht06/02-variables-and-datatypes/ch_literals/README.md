# Literals

What are the literals in the code snippet below?

The litterals here are 1,32,128 and the strings inside cout.

```cpp
int a = 1;
int b = 32;
int c = 128;

cout << "a = " << a << endl;
cout << "b = " << b << endl;
cout << "c = " << c << endl;
```